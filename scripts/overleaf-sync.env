# overleaf-sync.env.example
# scripts/overleaf-sync.sh 用の設定サンプル（このファイルを scripts/overleaf-sync.env にコピーして編集）
#
# 1) ブラウザで Overleaf CE (例: http://localhost) にログイン
# 2) DevTools → Application → Cookies から、セッションクッキーを確認
#    - 例: overleaf.sid=... または connect.sid=...
#    - _csrf も一緒に取得して "name=value; name2=value2" 形式で並べる
# 3) 下記を編集し、scripts/overleaf-sync.env として保存
#
# 例:
#   BASE_URL=http://localhost
#   COOKIE='overleaf.sid=s%3AXXXXXX....; _csrf=YYYYYYYY'
#   SLEEP_SEC=0
#   ROOT_FOLDER_ID=root-folder-id

# Overleaf CE のベースURL
BASE_URL=http://localhost

# ブラウザでログイン済みの Cookie を "name=value; name2=value2" 形式で
COOKIE='overleaf.sid=s%3AEUs0ZvdR6Tu8RCkdPXrGekM9AMzSlL1y.GSqQuP8FzcwBcDgiCvnpj8aLqbMAwwttiqmHn7%2B%2BsfQ'

# ファイル連続アップロードの間隔（秒）。大量アップ時のレート制限回避用。不要なら 0
SLEEP_SEC=0

# ルートフォルダID（既定: root-folder-id）
ROOT_FOLDER_ID=root-folder-id
